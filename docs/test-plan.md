# P0 Tool Test Plan

Before each case that changes `data/notes.json`, reset the fixture with `git checkout -- data/notes.json`. For the empty-data case, back up the fixture, replace it with `[]`, then restore the original file afterward. The offline/timeout case is simulated because the P0 tools are local-only and do not make network requests.

| id | tool | setup | input | expected | result | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| TC-01 | `search_notes` | Use the committed fixture; no reset needed. Reuse `examples/search_notes.json`. | `{ "query": "MCP", "limit": 5 }` | Successful response with `ok: true`; at least `note-001` (Model Context Protocol) is returned and no more than 5 matches are included. |  |  |
| TC-02 | `search_notes` | Use the committed fixture. | `{ "query": "   " }` | Input validation rejects the request because `query` is empty after trimming. |  |  |
| TC-03 | `list_categories` | Use the committed fixture. Reuse `examples/list_categories.json`. | `{}` | Successful response with `ok: true`; the response contains the available categories and a matching `count`. |  |  |
| TC-04 | `list_notes` | Use the committed fixture. Reuse `examples/list_notes.json`. | `{ "category": "AI" }` | Successful response with `ok: true`; only notes in the `AI` category are returned. |  |  |
| TC-05 | `list_notes` | Use the committed fixture. | `{ "category": "" }` | Input validation rejects the request because a provided category cannot be empty. |  |  |
| TC-06 | `search_notes`, `list_categories`, `list_notes` | Back up `data/notes.json`, replace its contents with `[]`, and restore it afterward. | `search_notes: { "query": "MCP" }`; `list_categories: {}`; `list_notes: {}` | Each tool handles an empty fixture without crashing: search and list return zero matches/categories/notes. |  |  |
| TC-07 | `search_notes`, `list_categories`, `list_notes` | Simulate an unavailable or hanging local fixture read by blocking/renaming `data/notes.json` or using a controlled read timeout; restore the fixture afterward. | `search_notes: { "query": "MCP" }`; `list_categories: {}`; `list_notes: {}` | Each tool fails cleanly with an error for unavailable/timeout fixture access; no partial or misleading success response is returned. |  |  |
| TC-08 | `add_note` | Reset the fixture before running this case. Reuse the shape from `examples/add_note.json`. | `{ "title": "Testing Notes", "content": "This verifies the add_note tool accepts valid data and stores it in the local collection.", "category": "Testing" }` | Successful response with `ok: true`; the new note is added with the expected title, content, and category, and the file remains valid JSON. |  |  |